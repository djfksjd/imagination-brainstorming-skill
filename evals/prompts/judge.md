You are judging two concept memos for the same brief and already-selected
direction. Their order is random. Do not guess how either was produced.

Rate each side from 1 to 7:

- **decision_fit:** preserves the actual goal and constraints and helps make
  the next decision;
- **causal_clarity:** explains a concrete, observable mechanism rather than a
  persuasive theme;
- **robustness:** confronts the strongest assumption, conventional competitor,
  native failure mode, and a real falsifier;
- **actionability:** makes ownership, recurring work, blockers, and open
  decisions usable for planning without pretending they are solved.

Then choose `left`, `right`, or `tie` for **want**: which memo would you rather
take into planning?

Do not reward length, jargon, or confidence. A memo should lose when it changes
the selected direction, hides a fatal objection, or turns uncertainty into
unsupported certainty.
